# Project Brief

Last updated: 2026-06-28

This is the single project truth document for the NYC TLC Green Taxi MLOps project. It replaces the older split state files `docs/project_state.md`, `docs/project_plan.md`, and `docs/notebook_structure.md`.

## Objective

Build a reproducible MLOps proof of concept using NYC TLC Green Taxi trip records. The course deliverable must show a deployment-oriented workflow, not only a modeling notebook:

- Data ingestion, profiling, validation, and documented quality findings.
- Feature engineering with a clear training-serving boundary.
- MLflow experiment tracking, model comparison, and model artifact logging.
- Kedro pipelines for stable workflows.
- Explainability artifacts.
- Serving API and Docker container.
- Drift or monitoring evaluation using later Green Taxi batches.
- Tests for implemented behavior.
- A final report of at most 6 pages with evidence generated by the project.

The project should stay understandable for a first-time MLOps student. Notebooks should teach the workflow visibly before code is extracted into reusable modules or Kedro pipelines.

## Current State

Project root: `C:\Users\Asus\Documents\MLOPS Project`.

Current data:

- Raw Green Taxi Parquet files are stored as one partitioned source under `data/01_raw/green_taxi/`.
- Available raw months in this workspace are 2024-01 through 2025-12 for modeling and 2026-01 through 2026-04 as the current drift holdout.
- `data/01_raw/taxi_zone_lookup.csv` is available for pickup/dropoff taxi zone enrichment.
- Derived artifacts already exist under `data/02_intermediate/`, `data/03_primary/`, `data/04_feature/`, `data/05_model_input/`, and `data/08_reporting/`.

Current notebooks:

- `notebooks/01_data_ingestion_profiling_and_validation.ipynb`: data loading, profiling, reference/analysis split, starter Great Expectations checks, and validation findings.
- `notebooks/02_data_preprocessing.ipynb`: data cleaning, target definition, feature engineering, optional Hopsworks upload, and intermediate file outputs.
- `notebooks/03_experiment_tracking_and_modeling.ipynb`: MLflow experiment tracking, baseline/model comparison, small Optuna tuning run, and registration flow for the current `tip_amount` regression task.

Current active Kedro registry:

- `ingestion`
- `data_unit_tests`
- `data_cleaning`
- `feature_engineering`
- `feature_selection`
- `split_data`
- `model_train`
- `data_prep`
- `training`
- `__default__`

Generated starter/example pipelines may still exist under `src/mlops_project/pipelines/`, but they are not part of the project contract and should not be registered or used in tests.

Current validation policy:

- Data-unit-test failures are warning-level report findings for now, not blocking gates.
- This is intentional because 2026 completeness failures are evidence for monitoring and data quality discussion, not automatic cleaning decisions.
- Do not silently relax or harden validation rules without documenting the reason.

Current known cleanup status:

- Historical markdown state files were consolidated into this brief on 2026-06-28.
- Pytest is configured to collect only project tests, not tests embedded in `class_materials/`.
- The stale generated Spaceflights data science test was removed from project tests.
- Git may report dubious ownership in this workspace; do not change global Git config unless the user explicitly approves it.

Current verification status:

- `uv run pytest tests -q` passed on 2026-06-28 with 10 tests.
- `uv run kedro registry list` passed on 2026-06-28 and lists only active Green Taxi pipelines plus `data_prep`, `training`, and `__default__`.
- `uv run kedro run --pipelines=model_train` passed on 2026-06-28; the focused run logged the model, reported validation RMSE 3.4767, saved the production model outputs, and completed one node successfully.
- `model_train` MLflow model logging uses explicit `cloudpickle` serialization because MLflow 3 defaults to `skops`, and the current fitted sklearn pipeline cannot be serialized in `skops` format. Only load those local MLflow model artifacts from trusted project runs.
- `kedro-mlflow` should use the same local SQLite backend as the project MLflow parameters (`sqlite:///mlflow.db`) instead of its default `mlruns` file store, which MLflow 3 blocks unless explicitly allowed.
- The `model_train` SHAP step should use a small configured sample, then pass a numeric dense array to `TreeExplainer`; the full fitted preprocessing output is a high-cardinality sparse matrix that is too large to densify.

## Decisions

Locked decisions:

- Dataset family: NYC TLC Green Taxi trip records.
- Package name: `mlops_project`.
- Dependency manager: `uv`.
- Core stack: Kedro, Great Expectations or simple documented checks, MLflow, Optuna after baselines, SHAP or permutation importance, FastAPI, Docker, and a justified drift method such as Evidently, PSI, JS divergence, or NannyML.
- Raw data layout: one Green Taxi partitioned source under `data/01_raw/green_taxi/`; do not duplicate raw files into separate train/drift folders.
- Current working target: `tip_amount` regression.
- Current modeling period: 2024-2025 Green Taxi data.
- Current drift holdout: available 2026 Green Taxi months.
- Current split parameter: `split_date: "2025-07-01"`.
- Hopsworks is optional and controlled by configuration. Keep credentials out of version control.
- Final notebook outputs should remain visible after reviewed reruns.

Evidence-dependent decisions that must stay explicit:

- Final production model and champion/challenger promotion decision.
- Final serving-time feature schema.
- Final cleaning and outlier thresholds.
- Final success metric thresholds.
- Drift method, alert thresholds, and whether any checks become blocking.
- Exact figures, tables, and claims included in the final report.

## Roadmap

1. Stabilize project truth and tests.
   - Keep `AGENTS.md` durable and this brief current.
   - Remove or quarantine generated starter tests and pipelines that are not part of the Green Taxi project.
   - Verify `uv run pytest tests -q` and `uv run kedro registry list`.

2. Finish data profiling and validation evidence.
   - Keep Notebook 1 readable and beginner-paced.
   - Show validation results and data quality findings visibly.
   - Keep Great Expectations checks tied to official code sets or observed profiling evidence.

3. Consolidate preprocessing and feature engineering.
   - Keep Notebook 2 understandable before extracting more code.
   - Identify serving-time versus post-trip columns.
   - Use Hopsworks only if credentials are locally configured; otherwise document the local Kedro catalog fallback.

4. Strengthen MLflow modeling workflow.
   - Keep a simple baseline before Optuna.
   - Log parameters, metrics, data period, model artifacts, and report artifacts.
   - Treat a newly better model as a challenger until evidence justifies promotion.
   - Use MLflow 3-compatible model logging and avoid noisy local environment variable recording.
   - Prefer `skops` when supported, but use explicit `cloudpickle` for the current sklearn pipeline until a `skops`-serializable artifact shape is available.

5. Add serving and containerization.
   - Build FastAPI endpoints for health, readiness, and prediction.
   - Use only serving-time features in request schemas.
   - Add a Dockerfile that installs pinned project dependencies.
   - Include a reproducible example payload.

6. Add explainability and monitoring.
   - Generate global feature importance or SHAP artifacts for the selected model.
   - Use local explanations only for realistic example predictions.
   - Compare reference and current periods for drift after the final split and features are clear.
   - Distinguish observed drift from artificial stress tests.

7. Prepare final delivery.
   - Rerun notebooks with visible outputs.
   - Ensure all report claims point to generated artifacts.
   - Keep the report within 6 pages and include package versions, risks, and production notes.

## Notebook Guidance

Do not create empty notebooks just to match a roadmap. Create or rename notebooks only when doing real work and after checking the current filenames.

Expected final topics:

- Data ingestion, profiling, and validation.
- Data preprocessing, feature engineering, and optional feature store.
- Experiment tracking and modeling.
- Serving and containers.
- Monitoring and drift.
- Explainability and report artifacts.

Before editing a notebook, inspect the matching practical class material listed in `docs/course_materials_review.md`. Use it for sequence and concepts, not as code to copy wholesale.

## Source Contracts

Local sources:

- `docs/source_materials/MLOps_project.pdf`
- `docs/source_materials/dataset_recommendations.md`
- `docs/source_materials/kedro_installation.pdf`
- `class_materials/`
- `docs/course_materials_review.md`

External sources to prefer for current guidance:

- Official NYC TLC trip data page.
- Official TLC Green Taxi data dictionary, trip records user guide, and Parquet guide.
- Kedro documentation for catalog, parameters, and pipeline registry.
- MLflow documentation for tracking, model logging, and model registry.
- Great Expectations documentation for validation concepts.
- FastAPI and Docker official docs for serving/container choices.
- Evidently, NannyML, or official/statistical references for drift choices.

Use online research for version-sensitive implementation decisions and cite official or primary sources in docs/report text when a claim depends on them.

## Verification

Use the existing uv-managed environment from the project root:

```powershell
uv run pytest tests -q
uv run kedro registry list
uv run kedro run --pipeline data_unit_tests
```

Run narrower tests first when changing one pipeline, for example:

```powershell
uv run pytest tests\pipelines\ingestion tests\pipelines\data_unit_tests tests\pipelines\data_cleaning -q
```

Do not use unscoped `pytest` if it collects `class_materials/`. Do not treat validation warnings as failures unless the project brief and report explain why a warning became a production gate.
